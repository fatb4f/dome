# Deploy Layout

Deployment assets are kept under this tree:

- `deploy/systemd/user/domed.service`: user service unit for `domed`

Use `scripts/install_domed_user_service.sh` to install/update the unit in
`~/.config/systemd/user/domed.service`.
