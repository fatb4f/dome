# /speckit.plan

Create or update `.specify/specs/<feature>/plan.md` using `.specify/templates/plan-template.md`.

Required:
- deterministic selection contract for `next(S)`
- evidence bundle definition for EXECUTE
- gating predicates for GATE
