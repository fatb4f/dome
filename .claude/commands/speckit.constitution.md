# /speckit.constitution

Update `.specify/memory/constitution.md` only.

- Treat it as SSOT.
- Do not modify `.specify/templates/*` unless explicitly asked to upgrade templates.
- Ensure the constitution enforces: PLAN → EXECUTE → GATE, evidence-derived state changes, deterministic next(S).
