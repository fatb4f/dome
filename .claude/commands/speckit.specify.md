# /speckit.specify

Create or update a feature specification (`.specify/specs/<feature>/spec.md`) using `.specify/templates/spec-template.md`.

Constraints:
- Focus on what/why; no implementation details.
- All requirements must name required evidence (tests/logs/artifacts).
